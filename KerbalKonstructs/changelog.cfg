KERBALCHANGELOG
{
	modName = KerbalKonstructs
	license = restricted
	author = KSP-RO team
	website = github.com/KSP-RO/Kerbal-Konstructs
	VERSION
	{
		version = 1.12.0.0
		versionKSP = 1.12.3
		CHANGE
		{
			change = What's Changed
			subchange = Fix incorrect vessel on launchsite warnings
		}
	}
	VERSION
	{
		version = 1.11.0.0
		versionKSP = 1.12.3
		CHANGE
		{
			change = What's Changed
			subchange = Scale fRadarRadius with body radius, check fRadarRadius for tooltip display
			subchange = Standardize csproj
			subchange = Move paths into GameData and out of the root PluginData
			subchange = Rework scenariomodule, group offset editor and other changes
		}
	}
	VERSION
	{
		version = 1.10.0.0
		versionKSP = 1.12.3
		CHANGE
		{
			change = What's Changed
			subchange = Allow custom editor windows
		}
	}
	VERSION
	{
		version = 1.9.0.0
		versionKSP = 1.12.3
		CHANGE
		{
			change = What's Changed
			subchange = API expansion, other changes
			subchange = Fix issues with static scale becoming 0
		}
	}
	VERSION
	{
		version = 1.8.11.0
		versionKSP = 1.12.3
		CHANGE
		{
			change = What's Changed
			subchange = Remove unrelated cginc and obj files from shader bundles
			subchange = Fix typo in tooltip
			subchange = Do not physically delete decal configs from gamedata
		}
	}
	VERSION
	{
		version = 1.8.10.0
		versionKSP = 1.12.3
		CHANGE
		{
			change = What's Changed
			subchange = Fix shader loading
		}
	}
	VERSION
	{
		version = 1.8.9.0
		versionKSP = 1.12.3
		CHANGE
		{
			change = What's Changed
			subchange = Deferred fixes/improvements
		}
	}
	VERSION
	{
		version = 1.8.8.0
		versionKSP = 1.12.3
		CHANGE
		{
			change = What's Changed
			subchange = Fix infinite loop on startup
		}
	}
	VERSION
	{
		version = 1.8.7.0
		versionKSP = 1.12.3
		CHANGE
		{
			change = What's Changed
			subchange = Add compatibility with Shabby and Deferred
		}
	}
	VERSION
	{
		version = 1.8.6.1
		versionKSP = 1.12.3
		CHANGE
		{
			change = What's Changed
			subchange = Fix KK startup breaking if a static references a body that doesn't exist ( #14 )
		}
	}
	VERSION
	{
		version = 1.8.6.0
		versionKSP = 1.12.3
		CHANGE
		{
			change = What's Changed
			subchange = Fix missing KSPUtil.ApplicationRootPath in texture load code. This should fix issues where KK doesn't work if used with the faulty PDLauncher bypass workaround.
			subchange = Add CI and KerbalChangelog file
		}
	}
}
